# Логика статуса и сортировки переработок (3 месяца × 2 половины)

## 0) Входные данные

### Периоды (6 слотов)
Мы храним фактические часы по половинам месяца:

- `Dec_1` = декабрь 1–15  (колонка C)
- `Dec_2` = декабрь 15–30 (колонка D)
- `Jan_1` = январь 1–15   (колонка E)
- `Jan_2` = январь 15–30  (колонка F)
- `Feb_1` = февраль 1–15  (колонка G)
- `Feb_2` = февраль 15–30 (колонка H)

Вектор периодов:
`P = [Dec_1, Dec_2, Jan_1, Jan_2, Feb_1, Feb_2]`

### Агрегации
- `Total = sum(P)`
- `DecTotal = Dec_1 + Dec_2`
- `JanTotal = Jan_1 + Jan_2`
- `FebTotal = Feb_1 + Feb_2`

### Счётчики регулярности
- `ActiveHalfCount = count(p > 0 for p in P)` — сколько половин месяца были рабочими
- `ActiveMonthCount = count(m > 0 for m in [DecTotal, JanTotal, FebTotal])` — в скольких месяцах была нагрузка

---

## 1) Полезные “объясняющие” метрики (для вывода/аналитики)

### 1.1 Последний раз работал (LastWorkedPeriod)
Определяем самый поздний период, где `p > 0`.

- `LastIndex = max(i where P[i] > 0)` (индексация 1..6)
- `LastLabel = labels[LastIndex]`

Где:
`labels = ["Дек 1–15","Дек 15–30","Янв 1–15","Янв 15–30","Фев 1–15","Фев 15–30"]`

> Смысл: кто работал “последним” — у того свежая усталость выше.

### 1.2 Пик нагрузки (PeakPeriod)
- `PeakValue = max(P)`
- `PeakIndex = first index i where P[i] == PeakValue` (при равенстве берём первый)
- `PeakLabel = labels[PeakIndex]`

> Смысл: помогает быстро понять, где была максимальная нагрузка.

### 1.3 Профиль нагрузки (Profile)
Классификация “почему так вышло”:

- если `ActiveHalfCount == 1` → `Profile = "разовый пик"`
- иначе если `ActiveMonthCount == 3` → `Profile = "хроническая (все 3 мес)"`
- иначе если `FebTotal >= 0.6 * Total` → `Profile = "свежая перегрузка"`
- иначе → `Profile = "равномерно"`

> Порог 60% для свежей перегрузки — настраиваемый параметр.

---

## 2) Статусы (4 уровня)

Цель: статус должен быть “управленчески справедливым”:
- если часов мало — человек должен “работать/можно грузить”
- хроническая регулярная работа — приоритет отдыха выше, чем разовый всплеск
- свежий всплеск влияет, но не должен “наказывать” маленькие суммы

### 2.1 Пороговые параметры (настраиваемые)
- `LOW_WORK = 60`  (если Total < 60 → можно работать)
- `MID_WORK = 80`  (если 60 ≤ Total < 80 → нормальная нагрузка)
- `FRESH_SHARE = 0.6` (доля FebTotal от Total, чтобы считать свежую перегрузку)
- `CHRONIC_HALVES = 4` (сколько активных половин считать “регулярной” работой)

> Параметры можно подстроить под команду/проект.

### 2.2 Логика выставления статуса (приоритет правил)

**Правило 0 — нет часов**
- если `Total == 0` → **Status = "⚡ можно работать"**
  - комментарий: человек не перегружен.

**Правило 1 — низкая суммарная нагрузка**
- если `Total < LOW_WORK` → **Status = "⚡ можно работать"**

**Правило 2 — умеренная нагрузка**
- если `LOW_WORK ≤ Total < MID_WORK` → **Status = "⚖️ нормальная нагрузка"**
  - комментарий: не отдыхать приоритетно, но и не “нагружать бездумно”.

**Правило 3 — высокая нагрузка: проверяем хроническую**
- если `ActiveMonthCount == 3` **и** `ActiveHalfCount ≥ CHRONIC_HALVES`
  → **Status = "🛑 срочно отдыхать"**
  - комментарий: хроническая усталость — приоритет отдыха №1.

**Правило 4 — высокая нагрузка: проверяем свежую перегрузку**
- если `FebTotal ≥ FRESH_SHARE * Total`
  → **Status = "🛌 лучше отдыхать"**
  - комментарий: свежая перегрузка — приоритет отдыха №2.

**Правило 5 — иначе**
- иначе → **Status = "⚖️ нормальная нагрузка"**

---

## 3) Сортировка людей (приоритет отдыха)

Цель: получить порядок “кого отпускать/разгружать первым”.

Мы сортируем по “PriorityScore” (чем больше — тем выше приоритет отдыха).
Сортировка нужна, чтобы люди с одинаковыми Total/похожими часами не “падали в одну группу”.

### 3.1 Компоненты приоритета

1) **Хроника (главный фактор)**  
Хронические должны подниматься выше свежих:
- `ChronicFlag = 1 if (ActiveMonthCount == 3 and ActiveHalfCount ≥ CHRONIC_HALVES) else 0`

2) **Свежесть (кто работал позднее — выше)**
- `LastIndex` из п.1.1 (1..6), чем больше — тем позднее.

3) **Свежие часы**
- `FebTotal` — если в конце было много часов, выше приоритет отдыха.

4) **Общий объём**
- `Total` — общий вклад, ниже по важности, чем хроническая регулярность (иначе “всплеск” победит хронику).

### 3.2 Формула PriorityScore (рекомендуемая структура)
Пример веса (можно менять):

- `PriorityScore =
    1000000 * ChronicFlag
  + 10000   * LastIndex
  + 10      * FebTotal
  + 1       * Total
`

Сортировка:
- по `PriorityScore` по убыванию (desc)

**Интерпретация:**
- любой хронический (`ChronicFlag=1`) гарантированно выше не-хронического
- среди хронических/нехронических порядок решает “кто работал последним”
- затем уточняет “сколько было свежих часов”
- и только потом общий Total

> Это устраняет проблему одинаковых Total: если люди одинаково отработали, но у одного последнее событие позже — он выше по отдыху.

---

## 4) Вывод строки “Предложение” (для UI)

Рекомендуемый формат:
`{Status} • последний раз: {LastLabel} • профиль: {Profile} • пик: {PeakLabel} ({PeakValue}ч)`

Пример:
`🛌 лучше отдыхать • последний раз: Фев 15–30 • профиль: свежая перегрузка • пик: Фев 1–15 (60ч)`

---

## 5) Примеры кейсов (контрольные)

### Кейc A: 0 часов
`P = [0,0,0,0,0,0]`
- Status: ⚡ можно работать
- LastLabel: нет (можно скрывать)
- Profile: нет данных/пусто

### Кейc B: мало часов, но всё в феврале
`P = [0,0,0,0,20,20]` Total=40, FebShare=100%
- Status: ⚡ можно работать (из-за порога LOW_WORK)
- Profile: свежая перегрузка (информативно), но статус не “отдыхать”

### Кейc C: свежий всплеск большой
`P = [0,0,0,0,60,60]` Total=120
- Status: 🛌 лучше отдыхать
- LastLabel: Фев 15–30
- Profile: свежая перегрузка

### Кейc D: хроническая равномерная
`P = [20,20,20,20,20,20]` Total=120
- ActiveMonthCount=3, ActiveHalfCount=6
- Status: 🛑 срочно отдыхать
- Profile: хроническая (все 3 мес)
- В сортировке будет выше свежего всплеска с тем же total.

---

## 6) Настройка под команду (что менять в первую очередь)
1) `LOW_WORK`, `MID_WORK` — чтобы “малые” нагрузки не попадали в отдых.
2) `FRESH_SHARE` — чтобы “свежей перегрузкой” считались только реальные всплески.
3) `CHRONIC_HALVES` — строгость определения “хроники”.
4) веса в `PriorityScore` — если важнее “последний раз” или “объём”.

---
